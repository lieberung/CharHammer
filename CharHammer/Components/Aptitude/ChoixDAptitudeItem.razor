@code {
    [Parameter, EditorRequired] public IEnumerable<AptitudeDto> Choix { get; set; } = null!;
    [Parameter] public bool PasDeTooltip { get; set; }
}
@for (var i = 0; i < Choix.Count(); i++)
{
    @if (i != 0)
    {
        <em class="insister text-decoration-underline px-1">ou</em>
    }
    <AptitudeItem Item=@Choix.ElementAt(i) PasDeTooltip=@PasDeTooltip />
}