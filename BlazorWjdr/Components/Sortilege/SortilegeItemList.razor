@using BlazorWjdr.Models

<ul class="list-inline">
    @foreach (var item in Items)
    {
        <li class="list-inline-item"><SortilegeItem Item="@item"/></li>
    }
</ul>

@code{
    [Parameter] public SortilegeDto[] Items { get; set; } = null!;
}