@using BlazorWjdr.Models
<ul class="list-inline">
    @foreach (var item in Items)
    {
        <li class="list-inline-item"><CompetenceAcquiseItem Item="@item"/></li>
    }
</ul>
@code{
    [Parameter] public CompetenceAcquise[] Items { get; set; } = null!;
}