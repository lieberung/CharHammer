@using BlazorWjdr.Models
@for (var i = 0; i < Choix.Length; i++)
{
    @if (i != 0)
    {
        <span>&nbsp;ou&nbsp;</span>
    }
    <CompetenceItem Item="Choix[i]"></CompetenceItem>
}
@code{
    [Parameter]
    public CompetenceDto[] Choix { get; set; } = null!;
}