@using BlazorWjdr.Models
@for (var i = 0; i < Choix.Length; i++)
{
    @if (i != 0)
    {
        <span>&nbsp;<em>ou</em>&nbsp;</span>
    }
    <TalentItem Item="Choix[i]"></TalentItem>
}
@code{
    [Parameter]
    public TalentDto[] Choix { get; set; } = null!;
}