@code {
    [Parameter, EditorRequired] public IEnumerable<LieuTypeDto> Items { get; set; } = null!;
}
<div class="list-btn-item">
    @foreach (var item in Items)
    {
        <LieuTypeItem Item=@item />
    }
</div>
