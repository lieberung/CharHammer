@using BlazorWjdr.Models

<tr>
    <td>
        <span class="btn btn-primary btn-item-action table-btn mr-1" @onclick="_ => AfficherFiche = !AfficherFiche">
            <OiIcon Icon="eye"/>
        </span>
        @Item.Nom
    </td>
    <td>@Item.Enc</td>
    @if (AfficherContenance)
    {
        <td>@Item.Contenance</td>
    }
    <td>@Item.Prix</td>
    <td>@Item.Dispo</td>
</tr>
@if (AfficherFiche)
{
    <tr>
        <td colspan="4">
            <div class="bulle p-3 mb-2">
                @Item.Description
            </div>
        </td>
    </tr>
}
@code {
    [Parameter] public EquipementDto Item { get; set; } = null!;
    [Parameter] public bool AfficherContenance { get; set; }
    
    private bool AfficherFiche { get; set; }
}