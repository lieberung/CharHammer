@using BlazorWjdr.Services

@inject AppState _appState

@code{
    private void OnPasswordChange(ChangeEventArgs args)
    {
        _appState.PretendreEtreUnDieu(Convert.ToString(args.Value) ?? string.Empty);
    }
}
@if (!_appState.JeSuisDieu)
{
    <span class="oi oi-lock-locked theme-color ml-4 mr-1" style="font-size: 1.7rem; top: 0.25em;" aria-hidden="true"></span>
    <input placeholder="Password" @onchange="OnPasswordChange" style="width:5em;"/>
}
else
{
    <span class="oi oi-lock-unlocked theme-color ml-4 mr-1" style="font-size: 1.7rem; top: 0.25em;" aria-hidden="true"></span>
}