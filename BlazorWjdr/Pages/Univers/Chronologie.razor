@page "/chronologie"

@inject ChronologieService Service
@inject AppState AppState
@code {
    private ChronologieDto[] _lines = null!;

    protected override void OnInitialized()
    {
        AppState.SetMainTopRowTitle("Chronologie");
        _lines = Service.AllChronologie();
    }
}
<table class="table table-striped">
    <thead>
        <tr>
            <th>Période</th>
            <th>Evènement</th>
            <th>Titre</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var chrono in _lines)
        {
            <tr>
                <td>@chrono.Periode</td>
                <td>@chrono.Resume</td>
                <td>@chrono.Titre</td>
            </tr>
        }
    </tbody>
</table>
